# Kaggle In Action

```
conda create --name kaggle python=3.7
conda activate kaggle
conda install pip numpy scipy pandas matplotlib sciki-learn ipython jupyhter seaborn pillow h5py scikit-image tqdm
pip install tensorflow-gpu==2.0
conda install -c conda-forge kaggle xgboost lightgbm opencv librosa
```
